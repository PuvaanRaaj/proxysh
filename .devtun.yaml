version: 1

daemon:
  listen_port: 8443
  log_file: ~/.config/devtun/devtun.log
  pid_file: ~/.config/devtun/devtun.pid

cert:
  ca_dir: ~/.config/devtun/ca
  cert_dir: ~/.config/devtun/certs
  ca_common_name: "devtun Local CA"
  days_valid: 825

domains:
  # - domain: myapp.test
  #   target: http://localhost:3000
  # - domain: api.test
  #   target: http://localhost:8080
  #   log: full
